import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    firstColor: {
      main: '#181818', 
    },
    secondaryColor: {
      main: '#000', 
    },
    secondaryTextColor: {
      main: '#FFF', 
    },
    ThirdColor: {
      main: "#ae946d"
    },
    bgLightGrayColor: {
      main: "#f6f6f6"
    },
    sectionPadding:{
      main : "30px",
    }
  },  
});
